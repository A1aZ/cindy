# 用量图表形状与日期选择：视觉证据索引

日期：2026-09-08。平台：Desktop / macOS arm64，Global 隔离开发版，Electron 41.10.3。PR 类别：有意可见变化。修复前基线：`760005b411406daca9016b2c33a6876c5d3de4f9`。

- 场景：真实 Cindy 的设置 → 用量历史；新建隔离 userData，以两组明确标记为 fixture 的模型数据生成 150 天历史，包含零值日和不同柱高。未修改正式数据库、真实用量记录或共享凭证。
- 截图来自运行中的 Electron 窗口；修复前在同一沙箱临时加载上述基线的三个图表/页面组件，采集后逐字节恢复候选代码。不是 SSR、静态设计稿或模拟图表页面。
- 预期变化：热力图按 `usage-heatmap-day` 登记显示，恢复 12px 网格、3px 间距；Token 柱按 `usage-token-bar` 登记显示并自适应宽度；同页增加等价单日选择，独立绘制 hover/focus/选中轮廓。灰阶和堆叠数据映射保持。
- 800px 窗口实测：修复前柱图区为 807px，下层可见区域约 410px；修复后 30 柱绘图区 clientWidth/scrollWidth 均约 410px，单柱约 10.77px，已无逐柱撑宽造成的横向滚动。热力格 12×12px，两个登记成员实测四角半径均为 2px。日期输入 190×36px，提交按钮高度 36px。
- 实机交互检查（Light/Dark）：热力格、柱体和日期表单进入同一天后概览结果一致；编辑日期草稿不提前过滤；筛选和 hover 不改变图元的尺寸、圆角及数据颜色。Tab 可达并出现 2px 焦点轮廓，Enter/Space 可筛选；独立 hover 轮廓为 1px。最底行图元到滚动视口边缘有 3px 留白，避免轮廓被裁。
- 非交互分支的几何与无按钮语义由组件单测覆盖；当前源码未找到 `HomeUsageDashboard` 的运行期挂载入口，因此未虚构首页实机验收。
- 栅格文件不入仓。Light/Dark 修复前后 PNG 与实测 JSON 已保存到工作区忽略目录 `.cindy/usage-chart-evidence-2026-09-08/`（含附件清单与生产文件 blob ID）；待在 PR 附件上传后回填稳定评论链接和最终代码 SHA。尚无公开附件链接时，不将证据交付状态记为完成。
- **人工视觉验收：待设计师批准。** 作者的采集、检查和测试不能替代此批准；本条不把整个 `desktop.settings` surface 标记为 migrated。
